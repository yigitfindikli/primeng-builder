{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsPrimengShell",
  "title": "Primeng Shell Options Schema",
  "type": "object",
  "properties": {
    "skipPackageJson": {
      "type": "boolean",
      "default": false,
      "description": "Do not add primeng dependencies to package.json (e.g., --skipPackageJson)"
    },
    "theme": {
      "type":"string",
      "default": "nova-light",
      "description": "The theme to apply"
    }
  },
  "required": []
}
